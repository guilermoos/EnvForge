#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import os
import sys

# Garante que podemos importar da subpasta envforge
sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from envforge.cli import run_cli

if __name__ == "__main__":
    run_cli()
